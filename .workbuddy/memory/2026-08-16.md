# 2026-08-16 工作记录

## v1.4.0 全栈交付审计（五部分）已完成
- 产出 `xionghan-chess-next/docs/AUDIT_v1.4.0_full.md`，严格按用户硬性五部分顺序：
  1. 文件结构梳理 + 文件功能对照表
  2. 代码深度审计（8 维度，含 v1.3.0 QA 47 条缺陷逐项复核、三端一致性、九项新功能、性能/安全/规范/架构/衍生缺陷）
  3. 测试执行结果（pytest 91 passed @ 项目 .venv；JS/JSON/compileall/git-diff 通过；Android Release 0 错）
  4. 交付产物清单 + 各端打包/部署步骤
  5. 验收结论 + 遗留风险（R-01~R-10）
- 关键结论：v1.4.0 交付包**无 P0 阻断**，9/9 新功能落地，47 条历史缺陷无 P0 残留。

## ⚠️ 关键项目事实：版本标识碎片化（必须记住）
- 工作树实际已是 **v1.5.0**：`src/xionghan_chess/__init__.py`、`<root>/pyproject.toml`、`web/js/app.js`(i18n import)、`web/index.html`(css)、`deploy/docker-compose.yml` 均标 1.5.0。
- `web/index.html:183` 另写 `app.js?v=2.0.1`（第三个版本号）。
- `release/v1.4.0-delivery/` 内部源码确为 **1.4.0**（冻结快照）。
- 已存在 `docs/CHANGELOG_1.5.0.md` / `DELIVERY_1.5.0.md` / `QA_REPORT_1.5.0.md`（2026-08-13），1.5.0 QA 声明 92 passed。
- **含义**：v1.4.0 交付包是历史冻结快照，live 代码已前进到 1.5.0。建议以 1.5.0 作为实际发布版本，或另建发布分支冻结 1.4.0。

## 三端一致性最高风险（安卓离线 JS vs Python Core）
- 安卓离线 `offline.js` 定义了 `no_progress_draw` 选项但 `settle()` 未实现 → 离线永不触发无进展和棋，而服务器/桌面会（正确性分歧，P1）。
- A-01（离线引擎无等价性测试）、A-05（翻转坐标空间不统一）同为离线引擎分歧点。
- 其余特色棋子边界无自动化等价回归。

## 1.6.0 版本规划（扫描 1.5.0 后编制）
- 产出 `xionghan-chess-next/docs/VERSION_PLAN_1.6.0.md`。
- 1.5.0 现状：工作树统一 1.5.0、**94 passed 全绿**；GAP 八项仅 **A-1(MCTS) 仍实验态**（纯 UCB1 模块 `core/mcts.py`，未接入默认难度、无 NN 训练流水线）；R-02(no_progress_draw 离线分歧) 已在 1.5.0 收口，R-01(版本碎片化) 已收口。
- 1.6.0 主题建议："AI 进化与三端一致性加固"——头号功能 F-1 把 MCTS 接为第五档 `master`（不启动 NN 训练，推至未来 2.0.0）；P1 含跨引擎等价性测试套件(F-3, 收尾 A-01)、广播增量(F-4)、安全 HTTPS(F-5)；P2 含战术爆炸裁剪、i18n、仓库卫生；R-09/R-10 收尾或待产品拍板。

## ⚠️ 新增关键冲突：2.0.0 孤儿产物（阻断级，已隔离）
- `release/` 存在 **2026-08-11 生成、被 git 跟踪、内部版本号 `__version__="2.0.0"`** 的**完整 2.0.0 发布树**（约 250 文件：完整 src/ + docs/DELIVERY_2.0.md + 资源 + build-onefile/ PyInstaller 产物）。
- **关键判定**：2.0.0 实为更早/废弃分叉——`core/ai.py` 仅 424 行(1.5.0=647)、`rooms.py` 376 行(1.5.0=504)、**不含 mcts.py**，功能少于 1.5.0。故"隔离"不丢能力。
- **M0 已执行(2026-08-16)**：`git rm --cached -r` 移除 147 个跟踪文件（仅索引，磁盘保留）；移入 `release/archive-experimental/2026-08-11-2.0.0-orphan/`；`.gitignore` 追加 `release/v1.5.0-delivery/` 与 `release/archive-experimental/`；校验 `git ls-files|grep 2.0.0`=0、src 仍 1.5.0。改动**未提交**，待用户审阅。版本倒挂已消除。

## 环境教训
- 必须用项目自带 `xionghan-chess-next/.venv/Scripts/python.exe` 跑 pytest（managed/workbuddy Python 缺 fastapi/pydantic，会 collection error）。
- Bash 的 `grep` 对中文 Unicode 区间 `[\x{4e00}-\x{9fff}]` 报 "Invalid range end"，需用 Grep 工具（ripgrep）处理中文。
