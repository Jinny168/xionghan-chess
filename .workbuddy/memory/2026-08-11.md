# 2026-08-11 工作日志

## 接手 xionghan-chess（Codex 迁移项目）
- 已读根目录 `handoff.md` 交接文档。真实在途工作树 = `xionghan-chess-next/`（handoff 描述的 `src/xionghan_chess`/`android`/`web`/`tests`/`docs`/`packaging`/`release`）。
- 根目录 `desktop/`、`web/`、`docker/`、`docs/` 为旧 1.1.0 实现；handoff 明确其不会被导入/覆盖，属遗留代码。
- 版本号漂移：代码内仍是 `version="1.1.0"`（app.py/pyproject），但 git 最新提交为「增加2.0版本」，release 删除记录引用 3.2.0；`xionghan-chess-next/release/` 现存 1.0.0 与 1.1.0 包。handoff 快照已过时。
- 在途未提交改动（与 handoff「主线可交付、无半成品」矛盾）：
  - `android/MainActivity.cs` +277/-16：含沉浸式全屏 `EnterImmersiveMode()`、`OnResume`/`OnWindowFocusChanged` 重绘、`RequestGameRedraw()`、离线同机模式 `LoadOfflineGame`/`CheckServerAsync`、导入导出文件桥 `OpenGameFile`/`SaveGameFile`。
  - 安卓图标改版：删 `mipmap-anydpi-v26/appicon*.xml`，替换全部 mipmap png，改 csproj。
  - `docs/`(AI_FIXES/ANDROID/DOCKER_DEPLOY/RUN_DESKTOP)、`pyproject.toml`、`README.md` 改动；`release/` 大量旧产物删除。
- 构建产物污染（handoff P1 真实存在且未处理）：`xionghan-chess-next/.gitignore` 仅忽略 `build/`、`dist/`，未忽略 `android/bin`、`android/obj`，二者已被 git 跟踪且有改动。根目录还有 `web.zip`(91MB) 等 clutter。
- 测试环境：`xionghan-chess-next/.venv` 存在，可跑 pytest。安卓真机/模拟器验证本环境无法执行，需用户侧完成。
- 待与用户确认：①在途未提交改动如何处理（提交检查点/保留继续/回退）；②本轮目标版本口径；③起步动作优先级。

## 新增：i18n / 中英文切换 规划（2026-08-11）
- 用户要求：仅关注 `xionghan-chess-next`，为匈汉象棋增加中英文切换，总结现有功能，联网搜缺失功能并出清单。
- 结论：**全项目无任何 i18n 机制，UI 文案 100% 硬编码中文**（core/service/web/desktop/android 六类位置）。
- 交付文档：`xionghan-chess-next/docs/i18n-language-switch.md`，含：①现有功能总结 ②可直接执行的"中英文切换"AI 提示词 ③联网调研缺失清单（A 类 i18n 强相关 + B 类通用象棋功能）。
- 棋子英文命名建议：将→Rook、车→Rook、马→Knight、相→Minister、仕→Guard、炮→Cannon、兵→Soldier；特色 尉/卫→Captain、射/䠶→Archer、檑/礌→Catapult、甲/胄→Armor、刺/伺→Assassin、楯/碷→Shield、巡/廵→Patrol。
