# AI Bug 修复清单

- AI 与玩家共用 `RulesEngine`，不再维护简化规则。
- 搜索使用局面副本，模拟走棋不会污染真实棋盘。
- 每层只遍历合法着法，阻止送将、越界、吃己方、吃楯和模式外棋子。
- 迭代加深保留上一层最佳结果，超时仍返回合法着法。
- 支持取消事件，重新开局或关闭窗口不会等待旧搜索。
- 桌面端 AI 思考锁阻止重复操作。
- 四档难度分别调整深度、时间预算和随机性。
- 升变、甲连线吃子和刺兑子进入统一状态转换。
- 捕获优先排序和 Alpha-Beta 剪枝减少无效搜索。
- 参考 Stockfish 搜索结构增加置换表、主变化优先、历史启发和 MVV-LVA 捕获排序。
- 叶节点使用静态吃子延伸搜索，降低刚好停在交换过程中的“地平线效应”。
- 无合法着法按失败局面计分，并优先选择更短的制胜路线。
- 无合法着法返回 `None`，由统一逻辑判断将死或困毙。
- `AI_TIME_SCALE` 可调整部署环境的统一思考时间倍率。
- 修复叶节点仍生成整盘全部合法着的问题；平静局面仅扩展吃子、升变和特殊吃子，被将军时才生成全部应将着。
- 使用选择性分支限制控制 13×13 大棋盘的搜索爆炸，优先保留吃子、将军、出子、中心控制和兵卒推进着法。
- 局面评分加入棋子位置、出子效率、兵卒推进、攻击压力、王安全和贴身掩护。
- 加入 killer move 排序和重复退回惩罚，减少同一棋子无目的往返。
- 入门和初级难度只在评分接近的高质量候选中产生误差，不再从全部合法着随机选择。

## 2.0 扩展修复

- 修复新增对话框标题被通用 i18n 选择器覆盖为“规则设置”的问题。
- 让子配置改用稳定棋位槽位，避免同类双车、双马无法分别选择。
- 服务端拒绝双方数量不等、模式外槽位、重复槽位和缺少主帅的开局配置。
- 随机先手只在权威服务端解析一次，快照同步最终结果，避免客户端各自随机。
- 翻转统一转换显示坐标与输入坐标，修复仅旋转画面后触控命中错位的常见实现缺陷。
- 观战令牌与玩家座位令牌分离，服务端明确拒绝观战通道的走棋消息。
- 云棋谱上传前通过现有 `.xhgame` 解析器校验，阻止无效文档进入账号数据。
- 账号密码使用随机盐和 310000 轮 PBKDF2-SHA256，不以明文或可逆形式保存。
- 题库加载检查重复 ID，新增测试逐题确认内置答案符合当前权威规则。
- 安装 WebSocket 运行依赖，修复仅安装基础 uvicorn 时升级请求被当作普通 HTTP 的问题。
